schema_version: 1
components:
  - id: nvidia-modelopt
    kind: quantization
    status: INTERFACE_ONLY
    platforms: {nvidia: NOT_TESTED, amd: UNKNOWN, cpu: UNKNOWN}
    upstream: [https://github.com/NVIDIA/Model-Optimizer]
    notes: >-
      An isolated, optional optimization-producer interface exists
      (src/opengrad/optimization). No ModelOpt execution has been run: FP8 PTQ, NVFP4,
      other PTQ, QAT/QAD, distillation, pruning/sparsity, and speculative-decoding
      training are NOT attempted, and no capability for Qwen/Qwen3.5-2B has been
      GPU-validated. Capability discovery reports UNKNOWN for every technique.
  - id: vllm-speculators
    kind: speculative_decoding
    status: REFERENCE_ONLY
    platforms: {nvidia: NOT_TESTED, amd: NOT_TESTED, cpu: UNKNOWN}
    upstream: [https://github.com/vllm-project/speculators]
    notes: Upstream reference only; no model, draft, acceptance, or throughput result exists.
  - id: dspark
    kind: inference_runtime
    status: REFERENCE_ONLY
    platforms: {nvidia: NOT_TESTED, amd: NOT_TESTED, cpu: UNKNOWN}
    upstream: [https://github.com/ModelTC/DSpark]
    notes: Upstream reference only; compatibility must be established by a future device preflight.
