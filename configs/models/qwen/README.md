# Qwen model configurations

Qwen3.5-2B is the study model and the only family with an executed renderer and trained
checkpoints; its declarative identity lives in [`registry/models.yaml`](../../../registry/models.yaml).
The concrete evaluation configs live in [`configs/evaluation/tool_calling/`](../../evaluation/tool_calling/).
This namespace holds no configuration files.
