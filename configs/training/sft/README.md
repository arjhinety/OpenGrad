# SFT configuration namespace

Reserved interface. Supervised fine-tuning is implemented and has been executed four times, but
experiment configs live in [`configs/experiments/`](../../experiments/) — for example the
definitive [`m0_sft_canonical_v2_final.yaml`](../../experiments/m0_sft_canonical_v2_final.yaml).
This namespace holds no configuration files.
