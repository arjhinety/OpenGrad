# Speculative-decoding configurations

Reserved. Speculative decoding is planned research, not implemented. It is in scope for
[Study 002](../../../docs/research/STUDIES.md). The benchmark system can
measure it when an implementation exists — see [`benchmarks/speculative/`](../../../src/opengrad/benchmarks/speculative/).
